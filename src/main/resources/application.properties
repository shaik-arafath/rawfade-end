# Server Configuration
server.port=${PORT:9090}

# Railway MySQL Database Configuration - Latest
spring.datasource.url=${DATABASE_URL:mysql://root:hCeVXbAFRayTLwphywfgAbjUWeXHytCP@ballast.proxy.rlwy.net:26089/railway?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:hCeVXbAFRayTLwphywfgAbjUWeXHytCP}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Database Initialization
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Production settings
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}
logging.level.com.cara=${LOG_LEVEL:INFO}

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:*}

# Railway MySQL Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
